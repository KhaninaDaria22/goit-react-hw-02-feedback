import styled from '@emotion/styled';
import { theme } from 'constans/theme'; 

export const FeedbackSection = styled.section`
    margin: 60px 0;
`;

export const FeedbacTitle= styled.h2`
  font-size: ${theme.font.gray};
  margin-bottom: 22px;
`;